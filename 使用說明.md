# SF6 USB Guard 0.1

這是針對你電腦上已定位的 HID 查詢逾時所製作的可逆防護程式。程式已編譯，模擬測試及真實裝置的唯讀識別已通過；實際暫停介面是否能消除遊戲凍結，尚未進行遊戲內驗證。

## 你方便時如何使用

1. 若使用 ZIP，先完整解壓縮，保留 `SF6-USB-Guard.exe` 與 `HidProbe.exe` 在同一個資料夾。這是免安裝版，使用 Windows 11 內建的 .NET Framework。
2. 先關閉《快打旋風 6》，再開啟 `SF6-USB-Guard.exe`。單純開啟視窗只會讀取裝置清單，不會啟用防護。
3. 可先按「只檢查接收器」。這只查詢四個目標介面的產品名稱，不停用裝置；遊戲執行中不提供此檢查。
4. 按「啟用防護並啟動 SF6」。此步才會要求 Windows 管理員權限、暫停四個介面，全部驗證成功後透過 Steam 啟動遊戲。
5. 第一次先在離線訓練模式確認鍵盤主要輸入、控制器正常，再測試 G705 開關／自然休眠。介面中的「防護中」表示四個指定介面已暫停，不代表遊戲效果已驗證。
6. 關閉遊戲後，約 8 秒自動還原本次變更。3 分鐘未偵測到遊戲啟動，也會自動還原。

防護期間，多媒體／音量鍵、系統控制鍵與廠商設定功能可能暫停。鍵盤的主要打字介面、另一個鍵盤介面與滑鼠介面不在停用清單中；G705、G435、控制器與整個 USB Hub 也不在停用清單。

## 這個程式如何保護

先前的約 40 秒凍結，快照顯示遊戲在 DirectInput → HID 裝置產品名稱查詢中等待。獨立查詢也重現了另一顆 `36B0:3002` 無線接收器四個輔助介面約 5 秒後失敗的現象。

本程式在遊戲啟動前暫停這四個介面，讓遊戲後續因 USB 變更重新掃描時，避開這組已知會逾時的介面。這是依診斷證據設計的防護假設，尚未經真實停用與遊戲重測證實。它不能保證任何 USB 故障都零卡頓，也不能使實際斷線的控制器繼續傳送按鍵。

識別範圍固定為你這顆接收器：

```text
USB\VID_36B0&PID_3002\19971217
```

僅允許以下 HID 子介面，並另外驗證其 HID Usage 與父裝置：

| 介面 | Usage | 可能暫停的功能 |
|---|---|---|
| MI_01 | FF60 / 0061 | 廠商設定 |
| MI_02 / Col02 | 0001 / 0080 | 系統控制鍵 |
| MI_02 / Col03 | 000C / 0001 | 音量／多媒體鍵 |
| MI_02 / Col05 | FF00 / 0001 | 廠商輔助功能 |

介面不完整、Usage 改變、裝置狀態異常或遊戲已在執行時，程式會拒絕啟用防護。其他品牌、序號、USB 父裝置及 Hub 不會被選中。

## 結束與還原

- 關閉主視窗後，已啟動的守護程序仍會等遊戲結束，再自動還原。
- 開遊戲前想取消，可以按「還原／結束防護」。遊戲執行中不切換介面，請先結束遊戲。
- 每次變更前會先寫入 `Data/session.json`。程式中斷後重新開啟，按「還原／結束防護」處理待還原項目。
- 原本就停用的介面會維持原狀；只還原這次原本啟用且嘗試變更的介面。
- 若接收器已拔除或換埠，還原紀錄會保留，請插回啟用防護時的原埠後重試。對戰中不自動追著新埠停用介面；視窗會顯示防護範圍不完整。
- 使用 Windows `CM_Disable_DevNode` 的非永久停用方式，未設定 `CM_DISABLE_PERSIST`。正常重新開機會重新啟用這類暫停；之後仍可開啟本程式整理還原紀錄。參考 [Microsoft 文件](https://learn.microsoft.com/en-us/windows/win32/api/cfgmgr32/nf-cfgmgr32-cm_disable_devnode)。
- 防護或待還原期間，請保留程式所在資料夾及 `Data`。不要刪除唯一的還原紀錄。

## 目前驗證範圍

- 26 項模擬測試通過：限定裝置、排除主要輸入／Hub、原本停用狀態保留、操作失敗回復、寫入紀錄失敗、復原、裝置消失、遊戲啟動競態、遊戲結束／取消／逾時流程。
- 真實電腦唯讀識別成功：四個目標介面、父裝置與 Usage 均相符。
- 操作視窗以不顯示於工作桌面的方式產生預覽並檢查。
- **未實際停用任何 USB 介面，未啟動遊戲測試，未宣告已消除短卡或所有凍結。**
- 無遊戲注入、無 DLL 替換、無驅動安裝、無自動開機啟動、無背景上傳。本機編譯的程式沒有數位簽章。

## 檔案

- `SF6-USB-Guard.exe`：主程式與按需啟動的管理員守護程序。
- `HidProbe.exe`：僅在按「只檢查接收器」時執行的名稱查詢工具。
- `Data/`：首次使用後建立的本機狀態與紀錄。
- `src/`、`build.ps1`：完整原始碼與建置腳本；重新建置需要 Visual Studio C++ Build Tools 和 Windows SDK。
- `驗證結果.txt`：本次模擬測試結果。
- `SHA256.txt`：本次兩個執行檔的雜湊。

執行模擬測試（不操作真實 USB）：

```powershell
Start-Process .\SF6-USB-Guard.exe -ArgumentList '--self-test', 'test-results.txt' -Wait
```
