# SF6 USB Guard 0.2 使用說明

## 開始

1. 完整解壓縮，保留兩個 EXE、說明檔及 Data 資料夾。
2. 先結束 SF6。若舊版正在防護，讓它完成還原並關閉舊版視窗。
3. 開啟 SF6-USB-Guard.exe。啟動只列舉目前 USB 裝置及其 HID 子介面，不查詢產品名稱、不停用裝置。
4. 右上角可選 English (US) 或繁體中文，程式會記住選擇。按「檢查並啟動 SF6」，允許管理員權限後開始重新檢查；需要時暫停符合條件的異常輔助介面，再自動啟動遊戲。
5. 首次先在離線訓練模式確認輸入正常，再測試 G705 休眠或 USB 變更。

沒有可處理的異常介面時，不修改裝置，仍會自動啟動 SF6，並顯示未更動 USB。檢查失敗、逾時、取消或還有待還原項目時不會啟動遊戲。

「重新檢查」只查詢，不停用、不啟動遊戲。檢查結果可在「詳細資訊」查看。按「檢查並啟動 SF6」會重新檢查，避免沿用過期結果。

## 會處理什麼

目前 USB 裝置下、已啟用的 HID 介面會接受產品名稱查詢。每次查詢由獨立子程序處理，最多同時四個；同一介面查詢至少 1.5 秒或產品名稱查詢超過 2.5 秒，才視為疑似過慢。只有符合用途範圍的疑似異常介面會重查，兩次都過慢才可停用。

可自動處理的範圍：

| 用途 | 條件 | 暫停期間可能影響 |
|---|---|---|
| 多媒體控制 | Usage 000C / 0001 | 音量、播放等按鍵 |
| 系統控制 | Usage 0001 / 0080 | 電源、睡眠等系統控制鍵 |
| 已知型號的廠商介面 | 36B0:3002 的 MI_01（FF60 / 0061）、MI_02 & Col05（FF00 / 0001） | 裝置設定與輔助功能 |

不限特定序號。另要求 HIDClass、沒有子裝置、Usage 唯一且 HID 與實體 USB 的 VID/PID 相符。主要鍵盤、滑鼠、遊戲控制器、觸控、USB 父裝置、Hub 與未知用途介面不會自動停用。其他型號的廠商自訂介面不會因為查詢慢就被視為可停用。

快速失敗、存取被拒、工具異常或無法確認用途，只保留檢查結果；不會當成停用依據。原本停用的介面保持原狀。

## 檢查期間

- 遊戲執行中不提供檢查或停用。若 SF6 在檢查中啟動，會中止查詢。
- USB 清單在檢查或套用前改變時，放棄本次計畫，請重新檢查。
- 最多檢查 128 個 HID 介面，整輪限時 90 秒；未完成時不套用部分結果。
- 可按「取消」。若已有變更，遊戲未執行時會嘗試還原。
- 遊戲中不追著新插入、重新插回或換埠的裝置停用；需要結束遊戲後重新檢查。
- 新版和舊版共用防護互斥鎖，不會同時套用兩份計畫。

## 結束與復原

- 關閉主視窗後，守護程序仍會等待遊戲結束，約 8 秒後還原。
- 3 分鐘未偵測到遊戲啟動，會自動還原。
- 只還原這次原本啟用、且曾嘗試暫停的介面。
- 中斷後重新開啟同資料夾的程式，先關閉 SF6，再按「還原」。
- 裝置拔除或換埠造成還原失敗時，會保留待還原紀錄；請插回原埠後重試。
- 請保留 Data/session.json 與備份，不要在待還原時刪除程式資料夾。
- 沒有設定 CM_DISABLE_PERSIST，正常重新啟動 Windows 會重新啟用此類暫停；程式仍可整理還原紀錄。
- v0.2 能讀取 v0.1 的限定裝置還原紀錄，但升級前應先用原版本完成還原。不同資料夾的 Data 不會自動搬移。

## 限制與驗證

記住 USB 清單不會阻止遊戲重新列舉裝置。本程式僅嘗試排除可確認用途的過慢 HID 輔助介面，不能保證所有 USB 變更都不凍結；也不能讓實際斷線的控制器繼續輸入。連線正常時未重現的間歇性問題可能不會被選中。

本版已進行模擬測試、真實電腦的唯讀裝置列舉與模擬 GUI 預覽。尚未實際停用新版選出的裝置，也未驗證遊戲內防凍結效果。測試結果見「驗證結果.txt」。

## 檔案與隱私

- SF6-USB-Guard.exe：主程式與按需啟動的管理員守護程序。
- HidProbe.exe：以零讀寫存取權開啟 HID，查詢產品名稱，不傳送輸入、輸出或 Feature report。
- Data/：本機裝置識別、檢查結果、日誌與還原紀錄，不會上傳。
- src/、build.ps1：原始碼、模擬測試與建置腳本。
- SHA256.txt：兩個 EXE 的 SHA-256。

程式無遊戲注入、無驅動安裝、無開機自啟，沒有數位簽章。
「請支持我繼續更新」只在點擊時以預設瀏覽器開啟 https://buymeacoffee.com/tananis ，不會自動開啟網站或限制程式功能。
